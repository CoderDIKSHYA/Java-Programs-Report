class DiscountEligibility {
    public static void main(String[] args) {
        int totalAmount = 1800;
        String result = (totalAmount > 2000) ? "Discount Applied" : "No Discount";
        System.out.println(result);
    }
}
