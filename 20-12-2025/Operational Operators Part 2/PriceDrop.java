class PriceDrop {
    public static void main(String[] args) {
        int oldPrice = 2500, newPrice = 2200;
        String result = (newPrice < oldPrice) ? "Price Dropped" : "No Price Drop";
        System.out.println(result);
    }
}
